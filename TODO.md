1.  In advanced all offsets should be visible.

2. There is no helpers on the set offsets and other items. Helpers should be for every item of the menu.
3. “Compress document” name may be should be revised? The name is not the best.
 
4. The info like page number, coordinates on the right bottom corner should be not on the separate field, but on the top of the opened page. (page should be down to the bottom of the window) 
5. Some elements have size of font less than others. All fonts(except number of splits on the page) should have one size of the font. Including helpers.

6. The design of the crop windows should be like before.  Dashed window, bigger square at the corner and middles of the sides, 30% bigger than main font number of the splits in the cercles. 

7. Ratio windows should be populated by initial documents or drawn/ created crop window not empty. Should be adjusted just after auto-detect, not after auto-detect and firs page change in navigation.
8. Status bar is still palatially drawn during some processing, freezing with others. Scroll of the left tab also freezy. 
9. Auto detect does not work correctly on the second press.
10. The navigation become slow.
11. The right button still magnify the page, not to set the crop window.

12. There is no name of the file/files on the top of the window. (len of the name should limit the name of the first file + … if there are many files.)
13. Strength in filters not active before filter applied, it can be selected before filter applied.
14. Items All/Odd/Even/Selected in menu blink than I press anything. Other items blinked with some presses.
15. After I change page selection the stateful button should drop the state.
16. If I selected split to 2/4 pages the button crop should label Split & Crop
17. Order for page split 1 top left, 2 bottom left, 3 top right, 4 bottom right.
